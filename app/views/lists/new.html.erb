
<div class="container">
  <h2 class="pb-5">Start a new list</h2>
  <div class="form">
    <%= simple_form_for (@list) do |f| %>
      <%= f.input :name %>
      <%= f.input :photo, as: :file %>
      <%= f.submit %>
    <% end %>
  </div>
</div>
